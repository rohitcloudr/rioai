# Production backend URL — used by the build to point /api/* requests at the
# deployed Express server. In local dev, leave this empty so the Vite proxy
# in vite.config.js forwards /api → http://localhost:8787.
#
# Example for production:
# VITE_API_URL=https://rio-backend.onrender.com
VITE_API_URL=
